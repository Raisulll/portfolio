# Resend API key — powers the contact form (app/api/contact/route.ts).
# 1. Create a free account at https://resend.com (sign up with raisul.dev@gmail.com).
# 2. Create an API key under "API Keys" and paste it below.
# 3. Copy this file to `.env.local` for local dev, and add the same var in your
#    Vercel project (Settings → Environment Variables) for production.
RESEND_API_KEY=
